# Production Environment Configuration Template
# Copy to .env.production and fill in actual values
# DO NOT commit actual secrets to version control

# Application Configuration
VERSION=1.0.0
ENVIRONMENT=production
REPLICAS=3
WORKERS=4
LOG_LEVEL=INFO

# Network Configuration
HTTP_PORT=8000
GRPC_PORT=50051
WS_PORT=8080
METRICS_PORT=9090

# Redis Configuration
REDIS_PASSWORD=<CHANGE_ME_STRONG_PASSWORD>

# Database Configuration
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30

# Cache Configuration
CACHE_TTL=3600

# SMTP Configuration (for Grafana alerts)
SMTP_HOST=smtp.company.com:587
SMTP_FROM=alerts@company.com

# Security Notice
# The following secrets should be managed via Docker Secrets or Kubernetes Secrets
# Never store actual values in this file:
# - redis_password
# - secret_key
# - jwt_secret
# - api_key
# - openai_api_key
# - anthropic_api_key
# - grafana_admin_password