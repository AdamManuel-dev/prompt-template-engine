/**
 * @fileoverview Test mocks for file system operations (compatibility layer)
 * @lastmodified 2025-08-22T15:05:00Z
 *
 * Features: Re-exports for backward compatibility
 * Main APIs: All mock classes maintained through barrel export
 * Constraints: Jest-compatible mocking, configurable behaviors
 * Patterns: Compatibility wrapper for split mock classes
 */

// Re-export everything from the barrel export to maintain backward compatibility
export * from './index';
